<Window
    x:Class="Voidstrap.UI.Elements.Dialogs.ComparePresetsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Compare FastFlags"
    Width="850" Height="650"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Grid Width="800" Height="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Description -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,16"
            FontSize="13"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            TextWrapping="Wrap"
            Text="Compare your current FastFlags with a preset or backup to see differences." />

        <!-- Selection -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Current FastFlags" FontWeight="Bold" Margin="0,0,0,8" />
                <TextBlock 
                    x:Name="CurrentCountText"
                    FontSize="12"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="0 flags" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Compare With:" FontWeight="Bold" Margin="0,0,0,8" />
                <ui:Button
                    x:Name="SelectPresetButton"
                    Click="SelectPreset_Click"
                    Content="Select Preset/Backup"
                    Icon="Document24" />
            </StackPanel>
        </Grid>

        <!-- Comparison Results -->
        <TabControl Grid.Row="2">
            <TabItem Header="Differences">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="DifferencesPanel" Margin="8">
                        <TextBlock
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="Select a preset to compare..."
                            TextWrapping="Wrap" />
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Only in Current">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock
                        x:Name="OnlyInCurrentText"
                        Margin="8"
                        FontFamily="Consolas"
                        FontSize="11"
                        Text="Select a preset to compare..."
                        TextWrapping="Wrap" />
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Only in Preset">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock
                        x:Name="OnlyInPresetText"
                        Margin="8"
                        FontFamily="Consolas"
                        FontSize="11"
                        Text="Select a preset to compare..."
                        TextWrapping="Wrap" />
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Statistics">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16">
                        <TextBlock x:Name="StatsText" FontSize="13" TextWrapping="Wrap" />
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
